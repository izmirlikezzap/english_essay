# Optional runtime settings
ESSAYS_DIR=data/essays

# Supported values: codex, claude
EVALUATION_PROVIDER=codex

# Used when EVALUATION_PROVIDER=codex
CODEX_MODEL=gpt-5

# Optional. Used when EVALUATION_PROVIDER=claude
CLAUDE_MODEL=
